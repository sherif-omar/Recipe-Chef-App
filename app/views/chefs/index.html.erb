<h2 class="text-center wight">Chefs <span class="badge"><%= Chef.all.count %></span></h2>


    <div class="row">
        <% @chefs.each do |chef| %>
        <div class="col-lg-3 text-center">
            <div class="thumbnail bootsnipp-thumb">
        	    <div>
        	    	<p class="pull-right view-counts hidden-md">
        	    		
        	    	</p>
        	    	<p class="lead snipp-title">
        	    		<%= "#{chef.frist_name} #{chef.last_name}" %>
        	    	</p>
        	    	<hr />
        	    </div>
                    <%= gravatar_for(chef,option: {:size => 120}) %>
            	<div class="caption">
            	     <%= link_to "View profile", chef_path(chef) , class: "row btn btn-success" %>
            	</div>
            </div>
        </div>
        <% end %>
    </div>
